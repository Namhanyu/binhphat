---
import bgImage from "../../../assets/images/projects-37.webp";
import { getLangFromUrl, useTranslations } from "../../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section
  id="hero"
  data-menu-color="white"
  class="h-screen max-md:max-h-[400px] bg-cover bg-center flex items-center max-md:items-end py-10 3xl:py-[2.5vw]"
  style={`background-image: url(${bgImage.src});`}
>
  <div class="container">
    <h2
      class="whitespace-pre-line text-5xl font-bold uppercase mb-4 3xl:mb-[1vw] max-md:text-3xl text-white leading-none"
      data-animate
      data-y="50"
    >
      {t("projects.title")}
    </h2>
    <div class="uppercase text-white" data-animate data-x="-50">
      <a href="/">{t("home.title")}</a>
      <span>-</span>
      <strong>{t("projects.title")}</strong>
    </div>
  </div>
</section>
